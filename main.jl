#main script for training and building  neural network
using Flux, using Images, using ImageSegmentation, using ObjectDetector
# define a function 'main' which is a convolutionnal neural network for detection and segementation as mask r cnn
function main
  #neural network
end
